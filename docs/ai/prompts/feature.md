# Feature Implementation Prompt

你正在哒哒助手仓库实现一个功能。

目标：

```text
<填写用户可观察的结果>
```

验收条件：

```text
<填写可验证的行为>
```

非目标：

```text
<填写本次明确不做的范围>
```

执行要求：

1. 阅读 `AGENTS.md`、需求和架构文档。
2. 找到现有模块边界和相似实现。
3. 先说明平台差异、失败路径和恢复策略。
4. 核心逻辑放入 `crates/`，Tauri Command 保持薄。
5. 增加针对验收条件的测试。
6. 执行相关格式化、检查和测试。
7. 汇报改动、验证结果和剩余风险。
