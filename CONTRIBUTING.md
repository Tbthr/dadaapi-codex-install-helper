# Contributing

提交改动前请阅读 `AGENTS.md` 和 `docs/`。

```bash
pnpm install
pnpm verify
```

Pull Request 应包含：

- 问题和目标
- 关键设计决策
- 测试结果
- Windows/macOS 影响
- 安全和网络恢复影响

向本仓库提交贡献即表示贡献内容按 Apache License 2.0 提供。提交前还应运行 `bash scripts/check-licenses.sh`；新增依赖必须具有与 Apache-2.0 分发兼容的许可证，并在包含用户可见资产或要求署名时更新 `THIRD_PARTY_NOTICES.md`。
